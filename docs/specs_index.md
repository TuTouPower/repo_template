# 当前生效 spec 清单

在表即生效。每个 task 收尾时写入或更新对应行（须已过黑盒）；废弃时整行删除，spec 移入 `docs/archive/specs/`。历史清单由 `docs/archive/specs/` 目录承载。

| slug | task 清单 | 最后更新时间 |
|------|----------|--------------|
| example_slug | t001 | 2026-07-21 |

<!-- 以上为示例行；实际项目使用时删除本示例，从真实 task 收尾时开始填写。 -->
