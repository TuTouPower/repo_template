# 已闭环待办历史

由 `scripts/repo_template/pending.py archive` 从 `docs/pending/todo/` 整文件迁入，文件名保持 `pNNN_{slug}.md` 不变。**只准新增**，禁止删除、改号或改写已归档条目。

`docs/pending/parked/` 条目属暂搁而非闭环，保留在主总账，不迁入本目录；需闭环时先 `revive` 回 `todo/`。

格式与命令见 `docs/pending/README.md`。
