# Task spec

## 背景

{为什么需要此变更。}

## 范围

- {本 task 包含什么。}

## 非范围

- {明确不做什么。}

## 验收标准

只写用户或调用方可观察行为，每条可独立验证。普通版本号、底层库和目录结构不作为验收标准；需要长期约束后续工作的技术选择写入 `docs/blueprint/decisions.md`。

- [ ] {可独立验证的行为结果。}

## 依赖与约束

- {前置依赖、平台、安全或兼容性约束；无则写“无”。}
